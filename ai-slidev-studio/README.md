# AI Slidev Studio

This project allows Frappe LMS users to create Slidev presentations using AI.

## Setup

1. Install dependencies:
    ```bash
    npm install
    ```

2. Run the development server:
    ```bash
    npm run dev
    ```

## Usage

Enter your prompt in the Frappe LMS interface to generate slides.
